# AI衣橱项目技术文档

## 1. 文档定位

本文档基于当前仓库中的 [PRD.md](/abs/path/C:/Users/Qodi/Desktop/codex/C端-AI衣橱/PRD.md) 与 [优秀代码风格.md](/abs/path/C:/Users/Qodi/Desktop/codex/C端-AI衣橱/优秀代码风格.md) 编写，用于指导本项目 MVP 阶段的技术设计、工程拆分与开发落地。

当前仓库尚未进入完整代码实现阶段，因此本文档描述的是：

- 已被 PRD 明确约束的业务边界；
- 推荐采用的工程实现方案；
- 为了保证后续代码可维护性而制定的模块与编码原则。

本文档不把未在 PRD 中明确的能力默认纳入开发范围。

## 2. 项目目标与范围

### 2.1 MVP 目标

本期 MVP 的技术目标不是构建一个完整的时尚平台，而是稳定交付以下核心能力：

- 用户完成注册登录与基础资料设置；
- 用户上传衣物图片，系统完成多单品识别、抠图、分类与标签生成；
- 用户确认后形成可检索、可编辑的数字衣橱；
- 用户可手动创建搭配，并记录喜欢、评分、舒适度与穿搭历史；
- 系统基于用户衣橱与输入条件生成 AI 搭配与今日推荐；
- 用户可记录实际穿搭，并在日历中查看历史记录。

### 2.2 本期明确不做

根据 PRD，本期技术方案默认不包含以下能力：

- 天气 API 接入；
- 系统日历读取；
- 每日主动推送推荐；
- 指定某件单品作为 AI 搭配核心单品；
- 商城推荐、电商购买链路；
- 独立运营后台；
- 自研基础模型、模型微调、大规模训练；
- 专业级人工抠图编辑器；
- 公开标签社区。

技术实现必须围绕“真实拥有的衣物数字化 + AI 辅助搭配”收敛，避免范围漂移。

## 3. 技术设计原则

### 3.1 设计原则

结合 PRD 和代码风格文档，项目实现应遵循以下原则：

- 先按业务模块拆分，再做技术分层；
- 数据模型先定义清楚，再编写服务逻辑；
- 模块低耦合、高内聚，只通过明确接口协作；
- 先实现直接可读的代码，再考虑抽象复用；
- 抽象只为解决真实重复，不为假设需求提前设计；
- 优先通过命名表达意图，减少无效注释；
- 复杂流程使用显式状态机，不依赖隐式布尔组合；
- 所有 AI 能力都视为不稳定外部依赖，必须有超时、重试和失败兜底。

### 3.2 代码风格落地要求

- 目录按业务域拆分，不按“utils 大杂烩”堆积；
- 单个函数只做一层明确职责；
- 服务层不直接拼装 UI 文案；
- 控制器不承载核心业务逻辑；
- 数据库字段、DTO 字段、前端状态字段命名保持一致；
- 对关键枚举统一收口，禁止字符串散落；
- AI 返回结果必须先做结构化校验，再进入业务流程。

## 4. 推荐技术架构

### 4.1 总体架构

本项目明确采用`前后端分离`架构，推荐采用 `Flutter 客户端 + 独立后端 API + 阿里云 OSS + 第三方 AI 服务` 的四层结构：

1. `Flutter App`
   负责页面展示、表单输入、图片选择、上传发起、任务轮询、状态反馈。
2. `业务后端 API`
   负责鉴权、业务规则、数据存储、AI 调用代理、上传签名、结果过滤与统一接口输出。
3. `阿里云 OSS`
   负责原图、抠图图、真人穿搭图、头像等私有资源存储。
4. `第三方 AI 服务`
   负责图像识别、抠图、标签生成、搭配文案与搭配结果生成。

### 4.2 推荐技术选型

PRD 已明确客户端使用 Flutter，因此建议采用以下技术组合：

- 客户端：`Flutter`
- Web API 服务端：`NestJS` 或 `Fastify + TypeScript`
- 数据库：`PostgreSQL`
- ORM：`Prisma`
- 对象存储：`阿里云 OSS`
- 异步任务：`BullMQ` 或同类任务队列
- 缓存与队列基座：`Redis`
- 鉴权：`JWT + Refresh Token`
- API 风格：`REST`

推荐 TypeScript 作为服务端主语言，原因不是“技术更先进”，而是：

- 与 Flutter 之外的产品文档、接口协议、枚举定义更容易统一；
- DTO、校验、任务状态、第三方返回结构更容易做静态约束；
- 更适合当前 MVP 所需的中等复杂度后端。

### 4.3 架构边界

本项目虽然采用前后端分离，但不建议在 MVP 阶段进一步拆成微服务。推荐保持`独立部署的单体后端 API`，并在代码结构上按业务域模块化，以便未来独立演进。

原因：

- 当前业务链路强相关，拆分微服务只会增加通信与部署成本；
- AI 任务、衣橱、搭配、穿搭记录的数据耦合较高；
- PRD 范围收敛，单体模块化足以支持 MVP。

### 4.4 前后端分离协作约定

为避免客户端与服务端职责混乱，前后端分离下应遵循以下约定：

- 客户端只通过 HTTP API 与后端通信，不直接访问数据库；
- 客户端不直接持有 AI 服务密钥；
- 客户端上传文件时，先向后端申请 OSS 上传签名，再直传 OSS；
- 客户端展示图片时，优先使用后端下发的临时访问地址；
- 服务端统一负责鉴权、权限校验、业务过滤、AI 结果校验与错误码定义；
- 所有面向 App 的数据结构以后端 DTO 为准，避免前后端各自维护一套字段语义。

## 5. 业务模块拆分

推荐后端按以下业务域拆分：

### 5.1 `auth`

职责：

- 注册登录；
- 登录态校验；
- Token 刷新；
- 退出登录；
- 账号注销。

### 5.2 `user-profile`

职责：

- 基础资料维护；
- 风格偏好维护；
- 模型优化授权开关；
- 个人中心相关信息读取。

### 5.3 `wardrobe-items`

职责：

- 单张与批量上传；
- 单品保存；
- 单品详情与编辑；
- 搜索、筛选、排序；
- 单品状态维护；
- 删除逻辑。

### 5.4 `image-processing`

职责：

- 原图上传签名；
- 图片任务创建；
- 多单品识别；
- 抠图；
- AI 标签结构化；
- 候选单品确认流转。

### 5.5 `outfits`

职责：

- 手动搭配创建；
- AI 搭配保存；
- 搭配详情；
- 搭配评价；
- 局部替换后的更新。

### 5.6 `recommendation`

职责：

- AI 搭配生成；
- 今日推荐生成；
- 近期穿搭去重规则；
- 候选单品过滤；
- AI 反馈记录。

### 5.7 `wear-log`

职责：

- 标记“今天穿了这套”；
- 真人照片上传；
- 心情、评分、备注记录；
- 穿搭日历查询。

### 5.8 `ai-gateway`

职责：

- 第三方 AI 服务接入；
- Prompt/Input 组装；
- 输出解析与校验；
- 超时、重试、熔断与日志记录。

### 5.9 `storage`

职责：

- 阿里云 OSS 上传签名；
- 阿里云 OSS 临时访问签名；
- 图片访问授权；
- 资源删除与清理。

## 6. 推荐目录结构

### 6.1 服务端目录

```text
server/
  src/
    app/
    modules/
      auth/
      user-profile/
      wardrobe-items/
      image-processing/
      outfits/
      recommendation/
      wear-log/
      ai-gateway/
      oss-storage/
    shared/
      config/
      database/
      enums/
      errors/
      logger/
      validators/
    jobs/
    main.ts
```

### 6.2 Flutter 目录

```text
app/
  lib/
    app/
    core/
      network/
      upload/
      theme/
      widgets/
    features/
      auth/
      onboarding/
      wardrobe/
      item-upload/
      item-detail/
      outfit/
      ai-stylist/
      today-recommendation/
      wear-log/
      calendar/
      profile/
```

目录划分应体现业务边界，而不是把不同页面公用逻辑全部塞入 `common`。

## 7. 核心数据模型

### 7.1 User

建议字段：

- `id`
- `nickname`
- `avatarUrl`
- `loginType`
- `gender`
- `ageRange`
- `city`
- `height`
- `weight`
- `bodyType`
- `stylePreferences`
- `allowModelTraining`
- `createdAt`
- `updatedAt`
- `deletedAt`

### 7.2 Item

建议字段：

- `id`
- `userId`
- `originalImageUrl`
- `cutoutImageUrl`
- `name`
- `categoryLevel1`
- `categoryLevel2`
- `primaryColor`
- `secondaryColors`
- `pattern`
- `brand`
- `size`
- `seasons`
- `styles`
- `scenes`
- `material`
- `fit`
- `purchasePrice`
- `purchaseDate`
- `customTags`
- `managementStatus`
- `wearableStatus`
- `createdAt`
- `updatedAt`
- `deletedAt`

建议枚举：

- `managementStatus`: `normal | idle | archived | discarded`
- `wearableStatus`: `wearable | washing`

### 7.3 Outfit

建议字段：

- `id`
- `userId`
- `name`
- `description`
- `scene`
- `style`
- `season`
- `source`
- `aiReason`
- `rating`
- `likeStatus`
- `comfortScore`
- `complimentCount`
- `createdAt`
- `updatedAt`

搭配与单品建议采用关联表：

- `outfit_items`
  - `outfitId`
  - `itemId`
  - `role`
  - `sortOrder`

原因：

- 后续局部替换更容易定位某个位置的单品；
- 避免把 `item_ids` 作为不透明数组长期堆在主表中；
- 更适合筛选、统计和约束。

### 7.4 WearLog

建议字段：

- `id`
- `userId`
- `outfitId`
- `wearDate`
- `photoUrl`
- `weather`
- `temperature`
- `scene`
- `mood`
- `rating`
- `note`
- `createdAt`

### 7.5 AI 任务表

PRD 对 AI 状态要求明确，因此建议单独建任务表：

- `id`
- `userId`
- `taskType`
- `status`
- `requestPayload`
- `resultPayload`
- `errorCode`
- `errorMessage`
- `sourceImageUrl`
- `relatedEntityId`
- `dedupeKey`
- `startedAt`
- `finishedAt`
- `createdAt`
- `updatedAt`

建议枚举：

- `taskType`: `item_recognition | item_cutout | item_tagging | outfit_generation | outfit_regeneration | outfit_replacement | today_recommendation`
- `status`: `pending | processing | success | failed`

## 8. 状态机设计

### 8.1 单品录入状态机

推荐采用显式状态流转：

```text
uploaded
→ recognizing
→ waiting_confirmation
→ saved
```

失败分支：

```text
upload_failed
recognition_failed
cutout_failed
```

注意：

- `cutout_failed` 不应导致原图丢失；
- `recognition_failed` 允许用户重试；
- `waiting_confirmation` 期间候选结果不能直接写入正式衣橱表。

### 8.2 AI 搭配状态机

```text
pending
→ processing
→ success
→ saved
```

失败分支：

```text
failed
timeout
filtered_empty
```

其中：

- `timeout` 是客户端展示语义；
- 服务端内部仍可统一落到 `failed`，但要带明确错误码。

### 8.3 单品状态规则

推荐候选池只允许进入以下单品：

- `managementStatus = normal`
- `wearableStatus = wearable`

以下单品必须被排除：

- `idle`
- `archived`
- `discarded`
- `washing`
- 已软删除单品

## 9. 核心业务流程

### 9.1 首次使用流程

```text
注册/登录
→ 完善基础资料
→ 设置风格偏好
→ 上传首批衣物
→ AI 识别与抠图
→ 用户确认
→ 形成数字衣橱
```

### 9.2 单品录入流程

```text
创建上传任务
→ 上传原图到阿里云 OSS
→ 创建识别任务
→ 识别 0~N 个候选单品
→ 对每个候选单品执行抠图与标签生成
→ 生成候选卡片
→ 用户确认/修改
→ 批量写入正式 Item
```

关键约束：

- 批量上传按图片逐张处理，不能互相拖垮；
- 一图多单品时，候选结果必须支持逐个删除与逐个保存；
- AI 结果不是最终数据，用户确认后才落正式表。

### 9.3 AI 搭配流程

```text
用户输入条件
→ 读取可穿单品
→ 读取用户偏好与历史反馈
→ 过滤不可用单品
→ 组装 AI 输入
→ 调用第三方 AI
→ 返回 3 套方案
→ 校验每套方案引用的 Item
→ 结果展示
→ 保存/喜欢/不喜欢/重生成/局部替换
```

### 9.4 今日推荐流程

```text
用户手动输入天气/温度/场景
→ 读取用户衣橱
→ 过滤不可穿单品
→ 读取最近 7 天穿搭
→ 组装今日推荐输入
→ AI 生成推荐
→ 保存或标记已穿
```

### 9.5 穿搭记录流程

```text
选择搭配
→ 标记今天穿了这套
→ 创建 WearLog
→ 上传真人照片
→ 填写评分/心情/备注
→ 保存
→ 在日历页查询历史记录
```

## 10. AI 集成设计

### 10.1 AI 集成原则

- 后端统一调用第三方 AI，客户端不直接暴露密钥；
- Prompt 或请求参数模板集中管理；
- 所有 AI 响应必须先做结构校验；
- AI 文本理由可直接展示，但单品引用必须经过服务端过滤；
- AI 是外部依赖，不得直接决定数据库写入结果。

### 10.2 识别类 AI 输入输出

输入建议包括：

- 用户上传原图；
- 基础任务类型；
- 当前需要的输出 schema。

输出建议结构：

- `detectedItems[]`
  - `name`
  - `categoryLevel1`
  - `categoryLevel2`
  - `primaryColor`
  - `secondaryColors`
  - `pattern`
  - `seasons`
  - `styles`
  - `scenes`
  - `material`
  - `fit`
  - `cropRegion`

### 10.3 搭配类 AI 输入输出

输入建议包括：

- 用户风格偏好；
- 当前场景、季节、天气、温度；
- 可穿单品列表；
- 最近 7 天穿搭摘要；
- 历史喜欢与不喜欢摘要；
- 历史评分摘要。

输出建议结构：

- `outfits[]`
  - `itemIds[]`
  - `title`
  - `reason`
  - `scene`

### 10.4 结果校验规则

服务端必须校验：

- 返回结果不能为空；
- 正常生成时必须是 3 套；
- 每套搭配至少包含有效单品组合；
- 引用单品必须属于当前用户；
- 引用单品必须存在；
- 引用单品不能为 `idle`、`archived`、`discarded`、`washing`；
- 校验失败时允许过滤后重组，不可用时视为失败或重新生成。

## 11. API 设计建议

以下为推荐的 REST 接口分组。

### 11.1 账户与资料

- `POST /auth/login`
- `POST /auth/logout`
- `POST /auth/refresh`
- `GET /me`
- `PATCH /me/profile`
- `PATCH /me/style-preferences`
- `PATCH /me/privacy`
- `DELETE /me`

### 11.2 衣物上传与确认

- `POST /uploads/image-sign`
- `POST /items/recognition-tasks`
- `GET /items/recognition-tasks/:taskId`
- `POST /items/recognition-tasks/:taskId/confirm`
- `POST /items/recognition-tasks/:taskId/retry`

### 11.3 衣橱管理

- `GET /items`
- `GET /items/:itemId`
- `PATCH /items/:itemId`
- `PATCH /items/:itemId/status`
- `DELETE /items/:itemId`

### 11.4 搭配与推荐

- `POST /ai/outfits/generate`
- `POST /ai/outfits/regenerate`
- `POST /ai/outfits/replace-item`
- `POST /ai/today-recommendation/generate`
- `POST /outfits`
- `GET /outfits`
- `GET /outfits/:outfitId`
- `PATCH /outfits/:outfitId/feedback`

### 11.5 穿搭记录

- `POST /wear-logs`
- `GET /wear-logs`
- `GET /wear-logs/calendar`
- `GET /wear-logs/:wearLogId`

API 设计要求：

- 写接口必须幂等可控，尤其是 AI 生成类接口；
- AI 处理中禁止重复提交同一请求；
- 列表查询统一支持分页。

## 12. 存储设计

### 12.1 数据库设计要求

- 所有主业务表带 `createdAt`、`updatedAt`；
- 需要逻辑删除的表带 `deletedAt`；
- 高频筛选字段建立索引；
- 组合查询优先围绕 `userId` 建索引；
- AI 任务表保留原始请求与结构化结果，便于追查异常。

建议索引：

- `items(user_id, deleted_at, management_status, wearable_status)`
- `items(user_id, category_level1, category_level2)`
- `outfits(user_id, source, created_at)`
- `wear_logs(user_id, wear_date)`
- `ai_tasks(user_id, task_type, status, created_at)`

### 12.2 对象存储设计

本项目对象存储明确采用`阿里云 OSS`。

建议目录约定：

```text
/users/{userId}/originals/
/users/{userId}/cutouts/
/users/{userId}/wear-logs/
/users/{userId}/avatars/
```

要求：

- 默认私有 Bucket；
- 不使用永久公开 URL；
- 客户端上传时使用后端签发的 OSS 直传凭证或签名参数；
- 客户端读取时使用后端签发的临时访问 URL；
- 删除账号时，数据库删除与 OSS 资源删除应形成一致性清理任务。

## 13. 客户端设计要求

### 13.1 页面状态

每个关键页面至少要区分：

- `idle`
- `loading`
- `success`
- `empty`
- `error`

涉及 AI 的页面还应增加：

- `processing`
- `retryableError`
- `waitingConfirmation`

### 13.2 客户端职责边界

客户端负责：

- 参数收集；
- 页面状态展示；
- 图片选择与上传；
- 轮询任务状态；
- 本地轻量缓存。

客户端不负责：

- 业务规则判定；
- AI 单品过滤；
- 权限判断；
- 核心标签写入逻辑；
- 第三方 AI 密钥管理。

### 13.3 交互要求

根据 PRD，客户端必须做到：

- 上传时明确显示上传状态；
- AI 处理中不出现假死；
- 上传失败可重试；
- 批量上传支持逐张反馈；
- 删除操作二次确认；
- AI 处理中禁止连续重复点击。

## 14. 安全与隐私

### 14.1 鉴权

- 所有用户资源必须按 `userId` 做服务端隔离；
- 不允许通过前端传入的 `userId` 直接决定查询范围；
- 图片访问必须经过签名或授权。

### 14.2 隐私

- 用户图片默认只用于当前功能处理；
- 未经明确授权，不得作为模型训练数据；
- 注销账号时需要删除衣橱、搭配、穿搭记录及相关图片资源。

### 14.3 审计与日志

必须记录：

- AI 调用时间；
- AI 错误码与错误信息；
- 关键写操作；
- 账号注销操作；
- 对象存储删除结果。

日志中不得打印：

- 明文 Token；
- 第三方 API Key；
- 用户隐私图片内容本体。

## 15. 异常处理与可观测性

### 15.1 AI 失败处理

按照 PRD，至少支持：

- 调用失败提示；
- 返回空结果视为失败；
- 返回不存在单品时过滤或重试；
- 单次 AI 超过 120 秒时提示“处理时间较长，请稍后重试”。

### 15.2 推荐错误码

建议统一错误码：

- `AI_TIMEOUT`
- `AI_EMPTY_RESULT`
- `AI_INVALID_ITEM_REFERENCE`
- `AI_PROVIDER_ERROR`
- `UPLOAD_FAILED`
- `FORBIDDEN_DUPLICATE_SUBMISSION`
- `ITEM_NOT_FOUND`
- `OUTFIT_NOT_FOUND`

### 15.3 监控指标

建议监控：

- 图片上传成功率；
- 单品识别成功率；
- AI 平均响应时间；
- AI 超时率；
- 今日推荐生成成功率；
- 局部替换成功率；
- 删除与注销任务完成率。

## 16. 测试策略

### 16.1 单元测试

优先覆盖：

- 单品状态过滤；
- 最近 7 天穿搭去重规则；
- AI 返回结果校验；
- 账号注销数据清理逻辑；
- 局部换单品逻辑。

### 16.2 集成测试

重点覆盖：

- 登录到上传再到确认保存；
- AI 搭配生成到保存；
- 今日推荐生成；
- 标记今日穿搭并生成 WearLog；
- 注销账号后数据不可访问。

### 16.3 Mock 策略

MVP 阶段建议对第三方 AI 做稳定 Mock：

- 成功返回；
- 空返回；
- 超时；
- 返回非法 Item ID；
- 返回不足 3 套。

这样可以把“系统逻辑正确”与“模型输出波动”分开验证。

## 17. 开发阶段建议

### 17.1 第一阶段

- 完成用户、衣物、搭配、穿搭记录的数据模型；
- 完成登录、基础资料、风格偏好；
- 打通图片上传与私有存储；
- 打通单品确认保存链路。

### 17.2 第二阶段

- 接入识别、抠图、标签 AI；
- 完成数字衣橱搜索、筛选、编辑、删除；
- 完成手动搭配。

### 17.3 第三阶段

- 接入 AI 搭配；
- 接入今日推荐；
- 完成穿搭日历；
- 完成账号注销。

开发顺序要优先保障主流程闭环，而不是先堆大量外围能力。

## 18. 结论

本项目的正确技术方向不是追求“大而全”的 AI 时尚平台架构，而是围绕 PRD 中已经明确的 MVP 范围，构建一个：

- 业务边界清晰；
- 数据模型稳定；
- AI 外部依赖可控；
- 状态机明确；
- 客户端与服务端职责清楚；
- 后续易维护、易扩展的模块化系统。

如果后续进入真实开发阶段，建议以本文档为基础继续补充：

- 数据库 ER 图；
- API 请求与响应示例；
- 第三方 AI Provider 适配层规范；
- Flutter 页面状态流转图；
- 上线环境配置与发布流程。
